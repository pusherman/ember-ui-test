import {
  fuzzyDate
} from 'fyibn-ui/helpers/fuzzy-date';

module('FuzzyDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = fuzzyDate(42);
  ok(result);
});
